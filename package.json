{
  "name": "@soyaos/website",
  "version": "0.1.0-alpha.0",
  "private": true,
  "type": "module",
  "description": "soyaos.ai — the marketing site for SoyaOS.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && pagefind --site dist && node scripts/check-public-web.mjs",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/tailwind": "^5.1.4",
    "astro": "^5.1.0",
    "tailwindcss": "^3.4.16"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "pagefind": "^1.5.2",
    "typescript": "^5.6.3"
  }
}
